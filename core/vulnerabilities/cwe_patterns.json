
{
    "CWE-121": {
      "type": "栈缓冲区溢出",
      "regex": "(strcpy|gets|scanf)\\s*\\([^)]*\\)",
      "severity": 95,
      "description": "栈缓冲区溢出是一种常见的缓冲区溢出漏洞，当程序向栈上的缓冲区写入数据时，写入的数据量超过了该缓冲区的边界，就会覆盖相邻的内存区域。这可能导致程序崩溃、执行任意代码、泄露敏感信息等严重后果，攻击者可以利用该漏洞来控制程序的执行流程，从而达到恶意目的。"
    },
    "CWE-134": {
      "type": "格式化字符串漏洞",
      "regex": "(printf|syslog)\\s*\\([^,]+,\\s*[a-zA-Z_]+\\s*\\)",
      "severity": 85,
      "description": "格式化字符串漏洞是一种常见的漏洞，当程序使用printf、syslog等函数时，如果格式化字符串中包含用户可控的变量，就可能导致格式化字符串漏洞。攻击者可以利用该漏洞来泄露敏感信息、执行任意代码等。"
    }
  }